<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Puerto Bjj" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    
    <%# SEO Meta Tags %>
    <meta name="description" content="<%= content_for(:description) || 'Academia de Jiu-Jitsu Brasileño, Boxeo y MMA en Puntarenas, Costa Rica. Únete a nuestra comunidad y aprende de los mejores.' %>">
    <link rel="canonical" href="<%= content_for(:canonical_url) || request.original_url %>" />

    <%# Open Graph / Facebook Meta Tags %>
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= content_for(:canonical_url) || request.original_url %>">
    <meta property="og:title" content="<%= content_for(:title) || 'Puerto Bjj' %>">
    <meta property="og:description" content="<%= content_for(:description) || 'Academia de Jiu-Jitsu Brasileño, Boxeo y MMA en Puntarenas, Costa Rica.' %>">
    <meta property="og:image" content="<%= content_for(:og_image) || image_url('home/test.jpeg') %>"> <%# CAMBIO TEMPORAL: Usando una imagen existente para evitar el crash. Reemplazar con una imagen og-image.png dedicada. %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "css-zero/reset", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "css-zero/variables", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "css-zero/utilities", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

   <body class="body-layout">
  
    <%= render "shared/scrolling_bar" %>
    <%= render "shared/header" %>
    <main id="main">
      <% if notice.present? %>
        <div popover class="flash" style="--flash-position: 5rem;" data-controller="element-removal" data-action="animationend->element-removal#remove" role="alert"><%= notice %></div>
      <% end %>
      <div class="container">
        <%= yield %>
      </div>
    </main>
    <%= render "shared/footer" %>

  </body>
</html>
